{
    "configurations": [
        {
            "name": "Launch Integrated Browser",
            "request": "launch",
            "type": "editor-browser",
            "url": "http://localhost:5500",
            "webRoot": "${workspaceFolder}",
        }
    ]
}